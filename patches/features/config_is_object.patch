Index: docroot/modules/contrib/features/src/FeaturesManager.php
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- a/modules/contrib/features/src/FeaturesManager.php	(revision )
+++ b/modules/contrib/features/src/FeaturesManager.php	(revision )
@@ -1335,7 +1335,6 @@
     // If config data is not specified, load it from the extension storage.
     foreach ($config_to_create as $name => $item) {
       if(is_object($item)) {
         $config_to_create[$name] = $item->getData();
       }
       if (empty($item)) {
