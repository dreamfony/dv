langcode: en
status: true
dependencies:
  config:
    - group.type.mailing_list
  module:
    - user
id: mailing_list-group_membership
label: 'Mailing list: Group membership'
description: 'Adds users to groups as members.'
group_type: mailing_list
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
