langcode: en
status: true
dependencies:
  config:
    - group.type.moderation
  module:
    - user
id: moderation-group_membership
label: 'Moderation: Group membership'
description: 'Adds users to groups as members.'
group_type: moderation
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
