<?php

/**
 * @file
 * Contains activity.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function activity_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the activity module.
    case 'help.page.activity':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Activity Entity') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function activity_theme() {
  $theme = [];
  $theme['activity_entity'] = array(
    'render element' => 'elements',
    'file' => 'activity_entity.page.inc',
    'template' => 'activity_entity',
  );
  $theme['activity_entity_content_add_list'] = [
    'render element' => 'content',
    'variables' => ['content' => NULL],
    'file' => 'activity_entity.page.inc',
  ];
  return $theme;
}

/**
* Implements hook_theme_suggestions_HOOK().
*/
function activity_theme_suggestions_activity_entity(array $variables) {
  $suggestions = array();
  $entity = $variables['elements']['#activity_entity'];
  $sanitized_view_mode = strtr($variables['elements']['#view_mode'], '.', '_');

  $suggestions[] = 'activity_entity__' . $sanitized_view_mode;
  $suggestions[] = 'activity_entity__' . $entity->bundle();
  $suggestions[] = 'activity_entity__' . $entity->bundle() . '__' . $sanitized_view_mode;
  $suggestions[] = 'activity_entity__' . $entity->id();
  $suggestions[] = 'activity_entity__' . $entity->id() . '__' . $sanitized_view_mode;
  return $suggestions;
}
